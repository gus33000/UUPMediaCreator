using System;
using System.Threading.Tasks;

namespace Microsoft.StoreServices.DisplayCatalog.Product
{
    public static class RequestHandler
    {
        //public static string GetProductEndpoint = "https://displaycatalog.mp.microsoft.com/v7.0/products/{0}?market=US&languages=en-US,en,neutral&fieldsTemplate=details";
        //public static string MSCV = "uPvMWf/GQEeAWT8M";

        //public static async Task<ProductApi> GetProduct(string ProductId)
        //{
        //    var endpoint = GetProductEndpoint;
        //    var cv = MSCV;

        //    endpoint = String.Format(endpoint, ProductId);

        //    var result = await endpoint.WithHeader("MS-CV", cv).GetJsonAsync<ProductApi>();

        //    return result;
        //}
    }
}